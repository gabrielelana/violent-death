// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("blast", "for blast support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("blast", "enable blast support", "no");

if (PHP_BLAST != "no") {
	EXTENSION("blast", "blast.c");
}

